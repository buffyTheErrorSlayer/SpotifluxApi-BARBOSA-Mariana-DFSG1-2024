{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center">
        <div class="col-4 mt-5">
            <h1>Artist</h1>
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ artist.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ artist.name }}</td>
                    </tr>
                </tbody>
            </table>
            <a href="{{ path('app_web_artist_index') }}">back to list</a>
            <a href="{{ path('app_web_artist_edit', {'id': artist.id}) }}">edit</a>
            {{ include('web/artist/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
